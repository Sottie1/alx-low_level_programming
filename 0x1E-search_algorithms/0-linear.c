 * @value: value to find
 *   *
 *     * Return: index whe#include "search_algos.h"

/**
  * linear_search - linear search
  * @array: pointer to first element in search array
  * @size: size of array
 re found or -1
  */

int linear_search(int *array, size_t size, int value)
{
	unsigned int i;

	if (e; i++)
	{
		printf("Value checked array[%d] = [%darray == NULL)
		return (-1);
	for (i = 0; i < siz]\n", i, array[i]);
		if (array[i] == value)
			return (i);
	}
	return (-1);
}
